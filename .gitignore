__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
site/

# Session working notes -- not meant to be published (contains IP/investigation
# details), kept locally only from here on.
IMPROVEMENT_PLAN.md

# Raw capture/scan output (data dumps, not source) -- the scripts that
# produce these live in tools/ and stay tracked.
tools/captures/
